\* Implemented fix (A + B'): single write, newline prefix over a torn tail, reader skips torn fragments. Serial = TRUE, power loss.
CONSTANTS
    Procs = {a, b, c}
    Mode = "one_write_prefix"
    Serial = TRUE
    PowerLoss = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK LedgerParses AckedDurable
CHECK_DEADLOCK FALSE
