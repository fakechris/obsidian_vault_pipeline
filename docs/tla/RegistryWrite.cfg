\* Current code (INV-686): the portal takes scheduler.lock; per-process tmp names. Expected: no error.
CONSTANTS
    Writers = {init, enable, portal}
    Portal = portal
    PortalLocked = TRUE
    UniqueTmp = TRUE
    MaxIno = 6
SPECIFICATION Spec
INVARIANTS RegistryParses NoLostUpdate
CHECK_DEADLOCK FALSE
