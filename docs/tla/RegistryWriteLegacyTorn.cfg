\* Before INV-686: unlocked portal + fixed tmp name. Expected: RegistryParses violated (a torn registry is published).
CONSTANTS
    Writers = {cli, portal}
    Portal = portal
    PortalLocked = FALSE
    UniqueTmp = FALSE
    MaxIno = 6
SPECIFICATION Spec
INVARIANTS RegistryParses
CHECK_DEADLOCK FALSE
