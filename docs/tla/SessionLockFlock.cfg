\* Current code (INV-685): File::try_lock on a never-deleted lock file. Expected: no error.
CONSTANTS
    Procs = {a, b, c}
    Ghost = g
    Mode = "flock"
    MaxIno = 7
SPECIFICATION Spec
INVARIANTS TypeOK Mutex
CHECK_DEADLOCK FALSE
