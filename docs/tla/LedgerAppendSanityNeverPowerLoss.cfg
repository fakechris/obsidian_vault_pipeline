\* Sanity: expected VIOLATED (NeverPowerLoss reachable under the implemented fix).
CONSTANTS
    Procs = {a, b, c}
    Mode = "one_write_prefix"
    Serial = TRUE
    PowerLoss = TRUE
SPECIFICATION Spec
INVARIANTS NeverPowerLoss
CHECK_DEADLOCK FALSE
