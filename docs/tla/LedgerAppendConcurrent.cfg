\* Code as written, two appenders at once (RunLock double-hold / unlocked writer), no crash needed.
CONSTANTS
    Procs = {a, b}
    Mode = "two_writes"
    Serial = FALSE
    PowerLoss = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK LedgerParses AckedDurable
CHECK_DEADLOCK FALSE
