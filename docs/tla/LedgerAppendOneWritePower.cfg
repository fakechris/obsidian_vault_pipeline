\* Fix A alone under power loss (torn tail, then the next append).
CONSTANTS
    Procs = {a, b}
    Mode = "one_write"
    Serial = TRUE
    PowerLoss = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK LedgerParses AckedDurable
CHECK_DEADLOCK FALSE
