\* Sanity: expected VIOLATED (a stale lock gets reclaimed).
CONSTANTS
    Procs = {a, b}
    Ghost = g
    Mode = "grave"
    MaxIno = 5
SPECIFICATION Spec
INVARIANTS NeverReclaims
CHECK_DEADLOCK FALSE
