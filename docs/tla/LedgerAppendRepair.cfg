\* Fix A + B: single write + truncate-unterminated-tail under the lock + tolerant reader; power loss.
CONSTANTS
    Procs = {a, b, c}
    Mode = "one_write_repair"
    Serial = TRUE
    PowerLoss = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK LedgerParses AckedDurable
CHECK_DEADLOCK FALSE
