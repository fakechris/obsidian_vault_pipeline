\* Fix A: one write_all(line + newline); concurrent appenders + SIGKILL.
CONSTANTS
    Procs = {a, b, c}
    Mode = "one_write"
    Serial = FALSE
    PowerLoss = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK LedgerParses AckedDurable
CHECK_DEADLOCK FALSE
