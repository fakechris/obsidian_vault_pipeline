\* Code as written, appenders serialized by run.lock, SIGKILL only.
CONSTANTS
    Procs = {a, b}
    Mode = "two_writes"
    Serial = TRUE
    PowerLoss = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK LedgerParses AckedDurable
CHECK_DEADLOCK FALSE
