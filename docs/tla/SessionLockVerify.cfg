\* Rejected patch: re-read the grave, link it back if live. Expected: Mutex violated with 3 processes.
CONSTANTS
    Procs = {a, b, c}
    Ghost = g
    Mode = "verify"
    MaxIno = 7
SPECIFICATION Spec
INVARIANTS TypeOK Mutex
CHECK_DEADLOCK FALSE
