\* Unique tmp names alone, portal still unlocked. Expected: NoLostUpdate violated, which is why the lock is needed too.
CONSTANTS
    Writers = {cli, portal}
    Portal = portal
    PortalLocked = FALSE
    UniqueTmp = TRUE
    MaxIno = 6
SPECIFICATION Spec
INVARIANTS RegistryParses NoLostUpdate
CHECK_DEADLOCK FALSE
