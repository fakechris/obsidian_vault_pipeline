\* Implemented fix (A + B'): single write, newline prefix over a torn tail, reader skips torn fragments. Serial = FALSE, power loss.
CONSTANTS
    Procs = {a, b, c}
    Mode = "one_write_prefix"
    Serial = FALSE
    PowerLoss = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK LedgerParses AckedDurable
CHECK_DEADLOCK FALSE
