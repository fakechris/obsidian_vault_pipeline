# Ops notes

Box/operator scratch for Grok × OVP. **Agent-facing rules live in [`skills/`](../../skills/)** (Notion-aligned pack).

Useful bits folded into skills:

- Capture drop folders → `skills/capture-drop`
- MCP connect (`ovp2 mcp --vault-root`, `OVP_VAULT_ROOT`) → root `mcp.json` + `skills/when-to-consult`
- Ask / citations → `skills/ask`, `skills/claim`

Do not put API keys, tokens, or provider secrets in these docs.
