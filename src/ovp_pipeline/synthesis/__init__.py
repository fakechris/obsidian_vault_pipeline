"""Synthesis layer (M13) — LLM-driven concept synthesis on top of
the deterministic truth projection.

* ``community_crystal`` — one synthesized markdown per Louvain
  community, persisted with lineage in the ``community_crystals``
  table for append-only versioning (BL-044).
"""
